// Declare a function with typed parameters and return value
// Create a function (e.g., add) that takes two numbers and returns their sum, with proper type annotations.
function add(num1, num2) {
    return num1 + num2;
}
console.log(add(2, 3));
